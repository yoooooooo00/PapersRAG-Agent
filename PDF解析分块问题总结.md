# PDF 解析与分块问题总结

## 影响最大的问题：正文被误判为表格

**现象**：作者信息、实验描述、图旁正文等被标为 `TABLE`，影响检索结果。

**通用解决**：

- 有规范表题的表格作为强证据；
- 无表题内容必须同时满足稳定列宽、至少两行数据行、连续尾部数值/缺失值列等结构条件；
- 不使用论文名、数据集名、指标名或固定表号判断。

**效果**：最终结果仅保留 5 张正式表格，普通正文不再误判。

## 其他问题与处理

| 问题 | 通用处理 | 最终效果 |
| --- | --- | --- |
| 跨页内容页码错误 | 分块时按来源页切分，不再继承章节起始页 | 页码与 PDF 实际页面一致 |
| 子章节被合并或正文被当标题 | 分离“编号标题识别”和“章节语义分类”，并校验标题式大小写、URL、句子特征 | 4.1、A.2、C.7 等层级正确；URL/参考文献句子不再成为标题 |
| 双栏图注遗漏 | 按列间距与图注句式切分行内图注 | Figure 1～8 共 8 个图注完整入库 |
| 多级表头左移、模型名拆列 | 用重复指标周期、尾部数值列和稀疏分组列恢复表格结构 | Table 1/2/4 对齐；Table 3 完整正确 |
| 特殊字体符号错误 | 对 Dingbat 字体做通用字形恢复 | `!/%` 恢复为 `✓/✗` |
| 英文跨行断词 | 安全合并英文断词，保留缩略词/模型名连字符 | 不再出现 `contin- uously` 等断裂文本 |
| 首页元数据重复 | 首页无章节片段标为 `FRONTMATTER`，由结构化元数据块统一承载 | 作者/单位信息不重复入库 |

## 最终验收

- 75 个块，索引连续；
- 5 张 TABLE，全部有表题；
- 8 个 Figure caption；
- 无伪表格、重复正文、错误章节标题、残留断词或 `!/%` 符号损坏；
- 通用测试、真实 PDF 严格回归和完整 Maven 测试均通过。
